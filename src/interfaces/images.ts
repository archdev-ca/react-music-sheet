export interface NoteImageMap {
  [key: number]: {
    width: string;
    height: string;
    image: string;
  };
}

export interface NoteCursorMap {
  [key: number]: {
    x: string;
    y: string;
    image: string;
  };
}
